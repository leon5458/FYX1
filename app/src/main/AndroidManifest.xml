<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.fyx.leon.fyx_leon.ui">

    <application
        android:name="com.fyx.leon.fyx_leon.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:allowBackup">
        <activity android:name=".FyxLoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"/>
        <activity android:name=".InformationActivity" />
        <activity android:name=".BombboxActivity" />
        <activity android:name=".ProgressBarActivity" />
        <activity android:name=".VerticalProgressBarActivity" />
        <activity android:name=".VerticalProBarTwoActivity" />
        <activity android:name=".InfoInputActivity" />
        <activity android:name=".InputActivity" />
        <activity android:name=".CalendarActivity" />
        <activity android:name=".ChangeActivity" />
        <activity android:name=".TableActivity" />
        <activity android:name=".ForgetPwActivity"/>
        <activity android:name=".CityActivity"/>

    </application>

</manifest>